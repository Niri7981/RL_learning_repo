本章总结几个核心问题。

## 1. 给定一个任务，如何写出它的 MDP 五元组？

将任务表示为

$$
M = \langle S, A, P, R, \gamma \rangle
$$

其中，$S$ 描述智能体可能处于的状态集合，$A$ 描述可选动作集合，$P(s' \mid s, a)$ 描述执行动作后的状态转移规律，$R(s, a)$ 或 $R(s, a, s')$ 描述即时奖励，$\gamma$ 描述未来奖励的折扣权重。

写 MDP 时，关键不是只把符号列出来，而是要说明每个要素在具体任务中分别对应什么。

## 2. 为什么 RL 优化的是折扣累积回报，而不只是即时奖励？

强化学习研究的是序列决策。某一步动作不仅影响当前奖励，也会改变后续状态，从而影响未来能够获得的奖励。因此，只优化即时奖励往往会导向短视策略。

折扣累积回报写作：

$$
G_t = \sum_{k=0}^{\infty} \gamma^k r_{t+k}
$$

它把当前和未来奖励统一成一个长期目标，并通过 $\gamma$ 控制未来奖励的重要程度。在无限期任务中，$\gamma < 1$ 还可以保证回报有限。

## 3. $G_t$、$V_\pi(s)$、$Q_\pi(s,a)$、$J(\theta)$ 分别在评估什么？

$G_t$ 是从时刻 $t$ 开始，沿一条具体轨迹实际得到的折扣累积回报。

$V_\pi(s)$ 是从状态 $s$ 出发并按照策略 $\pi$ 行动时，$G_t$ 的期望，用于评估状态价值。

$Q_\pi(s, a)$ 是在状态 $s$ 先执行动作 $a$，之后再按照策略 $\pi$ 行动时，$G_t$ 的期望，用于评估动作价值。

$J(\theta)$ 是参数化策略 $\pi_\theta$ 的总体期望回报，用于衡量并优化整个策略。

## 4. 贝尔曼期望方程和贝尔曼最优方程有什么区别？

贝尔曼期望方程用于评估给定策略 $\pi$，其动作选择由 $\pi(a \mid s)$ 加权平均：

$$
V_\pi(s) = \sum_a \pi(a \mid s)\left[R(s,a) + \gamma \sum_{s'} P(s' \mid s,a)V_\pi(s')\right]
$$

贝尔曼最优方程用于定义最优价值，它不再固定某个策略，而是在所有动作中取最大值：

$$
V^*(s) = \max_a \left[R(s,a) + \gamma \sum_{s'} P(s' \mid s,a)V^*(s')\right]
$$

前者回答的是“按照这个策略行动时，状态价值是多少”；后者回答的是“在最优行动下，状态价值最高能达到多少”。

## 5. DP、MC、TD 分别需要什么信息，什么时候更新，误差来源是什么？

DP 需要已知环境模型 $P$ 和 $R$，通过对所有可能动作和下一状态求期望来更新，误差主要来自迭代尚未收敛，或者来自函数近似。

MC 不需要环境模型，但需要等一整条 episode 结束后，才能用完整回报 $G_t$ 更新。它以真实回报为目标，估计无偏，但方差较高。

TD 不需要环境模型，也不必等 episode 结束，走一步就可以用

$$
r + \gamma V(s')
$$

来更新。它的方差较低，但因为使用了估计值 $V(s')$ 进行自举，会引入偏差。

## 6. TD Error 为什么会成为后续 Critic、深度 Q 网络和 GAE 的共同学习信号？

TD Error 写作：

$$
\delta = r + \gamma V(s') - V(s)
$$

它衡量的是当前价值估计与一步贝尔曼目标之间的差距。

Critic 可以用它更新状态价值函数；深度 Q 网络使用同样的自举思想构造 $Q$ 函数的训练目标；GAE 则把多个时间步的 TD Error 加权累积，用于估计优势函数。

因此，TD Error 本质上是把贝尔曼递推转化为可采样训练信号的一种基本形式。

## 7. 为什么 $Q(s,a)$ 能直接诱导动作选择？

$Q(s,a)$ 表示在状态 $s$ 选择动作 $a$ 之后的长期期望回报。

如果我们已经知道同一状态下各动作的 $Q$ 值，就可以直接比较它们的大小。若最优动作价值 $Q^*(s,a)$ 已知，则最优策略可以写成：

$$
\pi^*(s) = \arg\max_a Q^*(s,a)
$$

因此，$Q$ 函数不仅能评估动作，还能通过“选取价值最大的动作”直接给出决策规则。

## 8. 为什么参数化策略需要目标函数 $J(\theta)$？

参数化策略 $\pi_\theta(a \mid s)$ 的学习对象是参数 $\theta$。为了优化这些参数，需要定义一个以 $\theta$ 为自变量的目标函数：

$$
J(\theta) = \mathbb{E}_{\pi_\theta}[G_t]
$$

$J(\theta)$ 衡量当前策略的期望长期回报。策略梯度方法通过估计 $\nabla_\theta J(\theta)$ 来调整参数，使高回报轨迹中的动作概率增大，从而逐步改进策略。

## 9. 奖励塑形为什么可能加速学习，又为什么可能带来目标偏移？

奖励塑形通过增加中间奖励，使智能体在尚未到达最终目标前也能获得学习信号，因此可以缓解稀疏奖励导致的学习困难。例如，当状态更接近目标时给予额外奖励，就能引导探索方向。

但如果塑形奖励设计不当，智能体可能优化的是塑形信号，而不是原始任务目标，于是就会出现目标偏移。

势函数塑形

$$
F(s, s') = \gamma \Phi(s') - \Phi(s)
$$

在理论上可以保持最优策略不变，因此是一类相对更安全的奖励塑形形式。

## 后续章节如何使用

|后续章节|主要使用的本章对象|用法|
|---|---|---|
|第 4 章深度 Q 网络|$Q(s,a)$、$Q^*(s,a)$、$\arg\max_a Q(s,a)$、TD Target|用神经网络近似动作价值，并用自举目标更新 $Q$ 函数|
|第 5 章策略梯度|$\pi_\theta(a \mid s)$、$J(\theta)$、$\nabla_\theta J(\theta)$、$G_t$|直接优化参数化策略，提高高回报动作的概率|
|第 6 章 Actor-Critic|$V(s)$、TD Error、$J(\theta)$|用价值函数作为 Critic，为策略更新提供低方差信号|
|第 7 章 PPO|$V(s)$、优势函数、TD Error、策略目标|用 Critic 估计优势，并约束策略更新幅度|
|第 8 章以后大模型 RL|策略、奖励、回报、目标函数|把 token 生成看作序列决策，把偏好或验证信号转成优化目标|

## 小结

第 3 章建立了强化学习理论基础的基本结构：

1. 用 MDP 五元组定义序列决策问题。
2. 用折扣累积回报 $G_t$ 定义长期优化目标。
3. 用 $V_\pi(s)$ 和 $Q_\pi(s,a)$ 评估状态与动作。
4. 用贝尔曼方程揭示价值的递归结构。
5. 用 DP、MC、TD 说明价值可以如何被计算或估计。
6. 用 $J(\theta)$ 将参数化策略学习表述为优化问题。
7. 从数据来源维度区分算法：On-policy、Off-policy、Online、Offline。
8. 用奖励设计说明优化目标从何而来，以及为什么目标定义本身会影响学习结果。
